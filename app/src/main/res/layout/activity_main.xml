<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="advertizer.ibm.com.advertizer.activities.MainActivity">


    <Button
        android:id="@+id/advertise"
        style="@style/MainActivityButtons"
        android:layout_alignParentTop="true"
        android:text="@string/advertise_btn_start"/>

    <Button
        android:id="@+id/receive"
        style="@style/MainActivityButtons"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/advertise"
        android:text="@string/receive_btn_start"/>

    <ScrollView
        android:id="@+id/scroll"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/receive"
        android:background="@android:color/background_dark">

        <TextView
            android:id="@+id/scrollFill"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@android:color/white"/>
    </ScrollView>
</RelativeLayout>
